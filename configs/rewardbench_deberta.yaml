# RewardBench evaluation on BERT
name: rewardbench_deberta
bias_type: rewardbench

# Model settings
model: OpenAssistant/reward-model-deberta-v3-large-v2
model_path: OpenAssistant/reward-model-deberta-v3-large-v2
trust_remote_code: true

# Dataset settings
dataset_source: allenai/reward-bench-2
probe_size: 500
max_test_examples: null
split_seed: 42

# Evaluation settings
null_alpha: 1.0
batch_size: 8
max_length: 2048
device: cuda

# Output settings
save_probe: true

extra:
  split: test