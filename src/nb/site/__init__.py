"""Static review-site generation for the demographic-bias experiments.

Build inputs live here (in the research repo, beside the pipeline they document); the rendered
HTML is written into the separate public site repo via
``experiments/build_review_site.py --out``. Same split as
``experiments/export_paper_numbers.py``, which writes into the paper repo.
"""
